import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.arial.vocabulaire',
  appName: 'Vocabulaire',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
